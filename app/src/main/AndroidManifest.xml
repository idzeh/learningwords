<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.idzeh.learningwords">

    <application
        android:allowBackup="true"
        android:icon="@drawable/english"
        android:label="@string/app_name"
        android:roundIcon="@drawable/english"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WordsListActivity"></activity>
        <activity android:name=".ExamActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TopicSettingsActivity" />
        <activity android:name=".WordSettingsActivity" />
        <activity android:name=".CreateTopicActivity" />
        <activity android:name=".TrainCardsActivity" />
    </application>

</manifest>